import { Box } from '@chakra-ui/react';

const Hero = () => {
    return (
        <Box height={ 'calc(100vh)' } bgSize={ 'cover' } bgPosition={ 'center' } bgRepeat={ 'no-repeat' }>
            <p>Hello</p>
        </Box>
    );
}

export default Hero;